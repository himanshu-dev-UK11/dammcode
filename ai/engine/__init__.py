"""
AI Engine package.

The engine module is the central coordinator of MyCodingMaster.
It manages the entire lifecycle of a user request — from raw prompt
to final UI response — without containing any AI reasoning itself.
"""
